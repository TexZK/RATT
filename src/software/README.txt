Device and mapping for Atomix Production's Virtual DJ.

INSTALLATION
_ Windows: run the batch script "install_windows.bat"
_ MacOSX: run the shell script "install_unix.sh"

Obviously, Virtual DJ must be installed before these
XML files.
